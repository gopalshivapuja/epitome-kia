# Epitome Kia - Environment Variables
# Copy this file to .env.local for development or configure in Railway for production

# =============================================================================
# DATABASE (Required)
# =============================================================================
# PostgreSQL connection string from Railway or local Docker
DATABASE_URL="postgresql://user:password@localhost:5432/epitome_kia?schema=public"

# =============================================================================
# AUTHENTICATION (Required)
# =============================================================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-32-character-secret-key-here"

# Your domain URL (without trailing slash)
# Development: http://localhost:3000
# Production: https://epitomekia.com
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# EMAIL (Required for notifications)
# =============================================================================
# Resend API key from resend.com
RESEND_API_KEY="re_your_resend_api_key"

# =============================================================================
# ANALYTICS (Optional but recommended)
# =============================================================================
# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# =============================================================================
# GOOGLE MAPS & PLACES API (Required for maps and reviews)
# =============================================================================
# Google Maps/Places API key from Google Cloud Console
# Enable these APIs: Maps JavaScript API, Maps Embed API, Places API
# Restrict to your domains in Google Cloud Console for security
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# Optional: Separate server-side key for Places API (more secure)
# If not set, NEXT_PUBLIC_GOOGLE_MAPS_API_KEY is used
GOOGLE_PLACES_API_KEY="your-google-places-api-key"

# =============================================================================
# AI CHATBOT (Required for RAG chatbot)
# =============================================================================
# OpenAI API key from platform.openai.com
# Used for: GPT-4o-mini (chat) and text-embedding-3-small (RAG embeddings)
OPENAI_API_KEY="sk-your-openai-api-key"

# =============================================================================
# APPLICATION (Optional)
# =============================================================================
# Node environment (development, production, test)
NODE_ENV="development"

# Server port (Railway sets this automatically)
PORT="3000"

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit .env.local or .env files to git
# 2. Configure these in Railway dashboard for production
# 3. For local development with Docker, DATABASE_URL uses docker-compose service name
# 4. All NEXT_PUBLIC_* variables are exposed to the browser
